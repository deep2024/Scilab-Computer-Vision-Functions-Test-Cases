//read the image
img = imread("blob_detection.jpg");

// pass the image to open_blobAnalysis
output = blobAnalysis(img, "filterByThreshold", [1 10])

output = 
	
	Points: [0x0 constant]
   Size: [0x0 constant]
 
