//read the image
img = imread("blob_detection.jpg");

// pass the image to open_blobAnalysis
output = blobAnalysis(img, "filterByThreshold", [1 100], "filterByArea", [10 100])

output = 
	
	Points: [5X2 constant]
   Size: [5x1 constant]
 
